# MERID — Kalshi Swarm Intelligence Platform
# Python 3.11+

# ── Core Framework ──────────────────────────────────────────────
fastapi==0.135.1
uvicorn[standard]==0.41.0
pydantic[email]==2.12.5
pydantic-settings==2.13.1
python-multipart==0.0.22
starlette==0.52.1

# ── Social Media / Notification Bots ───────────────────────────
tweepy>=4.14,<5                  # X/Twitter OAuth 1.0a posting
python-telegram-bot>=20,<22      # Telegram Bot API

# ── HTTP Clients ────────────────────────────────────────────────
httpx==0.28.1
aiohttp==3.10.11
requests==2.32.5
websockets==14.2
websocket-client==1.9.0

# ── News / RSS Feed Parsing ────────────────────────────────────
feedparser>=6.0,<7                 # RSS/Atom feed parsing for news_stream

# ── Data & Numerics ────────────────────────────────────────────
numpy==2.4.4
pandas==3.0.1
scipy==1.17.1

# ── Logging ─────────────────────────────────────────────────────
structlog==25.5.0
python-json-logger==4.0.0
loguru==0.7.3
asgi-correlation-id==4.3.1

# ── Configuration & Environment ─────────────────────────────────
python-dotenv==1.0.1
python-dateutil==2.9.0
pytz==2024.2

# ── Resilience & Retry ─────────────────────────────────────────
tenacity==9.1.4
circuitbreaker==2.1.3
pybreaker>=1.0,<2                  # Circuit breaker (used by core/resilience.py)

# ── Task Queue ─────────────────────────────────────────────────
celery>=5.3,<6                     # Distributed task queue (core/celery_tasks.py)

# ── Security & Auth ─────────────────────────────────────────────
cryptography==44.0.0
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
email-validator==2.3.0

# ── API Middleware ──────────────────────────────────────────────
slowapi==0.1.9
prometheus-client==0.24.1
cachetools==5.5.0

# ── System Monitoring ───────────────────────────────────────────
psutil==7.2.2

# ── CLI ─────────────────────────────────────────────────────────
click==8.3.1
rich==14.3.3
typer==0.24.1

# ── Async Utilities ─────────────────────────────────────────────
anyio==4.7.0
asyncio-throttle==1.0.2

# ── Database ─────────────────────────────────────────────────────
asyncpg==0.30.0                     # PostgreSQL async driver (replaces SQLite for fills ledger)
aiosqlite==0.20.0                   # SQLite async driver (fallback)

# ── Serialization ──────────────────────────────────────────────
jsonschema==4.23.0
ormsgpack==1.12.2

# ── Testing ───────────────────────────────────────────────────────
pytest==8.3.5
pytest-asyncio==0.25.0
pytest-timeout==2.3.1
hypothesis==6.130.5
httpx-ws==0.9.0

# ── Code Quality ────────────────────────────────────────────────
black==24.10.0
flake8==7.1.1
mypy==1.19.1

# ── Optional: Observability (install if using Jaeger tracing) ───
# opentelemetry-api==1.24.0
# opentelemetry-sdk==1.24.0
# opentelemetry-exporter-jaeger==1.21.0
# opentelemetry-instrumentation-fastapi==0.42b0

# ── Optional: Graph DB (install if using Neo4j) ────────────────
# neo4j==6.1.0

# ── Optional: Cache/PubSub (install if using Redis) ────────────
# redis==7.2.1
