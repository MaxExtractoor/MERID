MERID v0.1.0 (2026-01-31)

Highlights
- Core orchestrator with SSE streaming
- Trading & Risk engine with circuit breaker and pre-trade checks
- Admin lockdown + token auth
- Background job queue + in-process metrics and `/metrics` endpoint
- Shared schemas (Pydantic v2) and exported schema artifacts
- E2E simulation harness and CI smoke checks
- Audit logging with optional file-backed persistence
- Docker and CI publishing workflows (publish-on-tag)

Full changelog: see `docs/CHANGELOG.md` under the v0.1.0 section.
