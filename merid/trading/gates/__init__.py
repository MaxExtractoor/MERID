"""Trading gate modules for MERID.

Each module in this package exposes a gate function that takes raw
or pre-filtered signals and applies one or more acceptance criteria,
returning only the signals that pass all checks.
"""
